# project-epistemic-bound — start the three builders

**Owner:** Anthony Vasquez Sr.  
**Assignment:** build the project defined in `BUILD_SPEC.md`.  
**Team:** Claude Code, Grok, Codex. **These are builders, not experimental subjects.**

Place `BUILD_SPEC.md` and this file in the intended starting project folder. The inherited starting location is `~/Desktop/untitled`; Claude must inspect it before changing it. Do not manually create three separate repositories.

## Start Claude Code first

Paste this into the Claude Code terminal:

```text
Read BUILD_SPEC.md in full. You are the integration lead and runtime builder
for project-epistemic-bound. Anthony is moving from brainstorming to building.
You, Grok, and Codex are the construction team, not the subjects of an experiment.

Perform the checked S0 preflight, rename Desktop/untitled only when its actual
state matches the spec, initialize the local repository, and create separate
Claude/Grok/Codex worktrees. Preserve existing files; do not stage the entity vault
or private papers. Publish the schema contract and exact worktree paths so the
other two builders can start without Anthony relaying technical details.

Own the agent runtime, context builder, candidate profiles, and local model
adapter. Coordinate Grok's broker/recorder lane and Codex's cockpit/acceptance
lane. Work through S1–S5, merging reviewed local commits and recording test
receipts. Deliver the working synthetic report-repair application first, then
a separately instantiated local model. No broad agent fan-out, no publication,
no live resources, and no silent cloud fallback. Do not stop at another plan.
```

## Start Grok in its assigned worktree

Paste after Claude has published the S0 worktree/schema receipt:

```text
Read BUILD_SPEC.md. You are the broker, resource-engine, and flight-recorder
builder for project-epistemic-bound. You are not an experimental subject.
Use the build/grok worktree Claude established. Own scoped grants, exact
approvals, synthetic resource transactions, pre-gate attempt logging, effect
receipts, verification/replay, crash recovery, and the durable review/hold route.

The key case: an authorized report edit may still be dishonest. Do not use a
hidden expected answer to make that edit impossible and then call the result
agent integrity. Preserve both authorized dishonesty and externally blocked
unauthorized attempts as distinct outcomes. Build and test your lane, review
Codex's fixture/UI boundaries, and communicate through the project coordination
files. Return exact commits, test receipts, and a clean handoff.
```

## Start Codex in its assigned worktree

Paste after Claude has published the S0 worktree/schema receipt:

```text
Read BUILD_SPEC.md. You are the cockpit/CLI and acceptance-engineering builder
for project-epistemic-bound. You are not a subject, and the other construction
seats are not your test subjects. Use the build/codex worktree Claude established.

Build the operator interface, CLI presentation, six synthetic scenario families,
grading fixtures, frame-pair validator, acceptance matrix, and runbook. Test
software using explicit scripted actors before model comparisons. Make honest
repair, authorized concealment, blocked unsafe attempts, protocol failure,
missing evidence, escalation, and correction visibly distinct. Review Claude's
runtime/provider boundary. Return working features and actual test receipts,
not another research proposal.
```

## What should come back

A working local application at `~/Desktop/project-epistemic-bound`, an offline
scripted demonstration with verifiable receipts, a separately configured local
agent, an inspectable authority/evidence boundary, and the tested operator
runbook. `BUILD_SPEC.md` contains the exact stage exits and acceptance requirements.

The behavioral papers remain source proposals, not automatically enacted Temple
law. Build and verify the instrument now. A broad behavioral pilot, publication,
paid inference, or live-system integration is a separate explicit action.
