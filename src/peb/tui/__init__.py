"""Terminal cockpit (`peb tui`): a second presentation over the SAME operator boundary as the browser workroom.

The production transport is an authenticated client of the existing loopback web seam (session cookie, CSRF,
exact origin, the one-use hosted preview token). Nothing in this package imports the writable repository, a
provider, the reference monitor or the executor; the terminal is an operator lens over the recorder, never a new
authority. Design input: docs/design/peb_cockpit_tui_design.md (received 2026-09-12; used where it fits, not as a
specification). Decisions: ADR-019.
"""
