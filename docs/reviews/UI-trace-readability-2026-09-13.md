# Trace readability — 2026-09-13

Assistant SELF-REVIEW, with a bounded agent read of the diff. This is not an
independent acceptance verdict and promotes no matrix row.

Anthony found that the reference-monitor reason could strand its last letter,
and narrow views concealed the last two lane headings behind horizontal scroll.
The trace now inserts DOM `wbr` opportunities after underscores and displays a
wrapping actor overview outside that scroll region. The five lanes keep stable
positions; quiet actors retain an explicit zero count. Counts cover the full
snapshot, including events past the trace's current page. Unknown actors remain
labeled in the trace and are grouped separately in the overview.

Named assertions in `tests/browser/instrument.cjs` check exact copied/title reason
text, one-line `mismatch` components, positive visible bounds for all five actors,
zero and off-page counts, scroll-hint visibility, and reachability of the last two
lanes. Hostile reason text creates only intended `wbr` elements, never HTML from
the record. The agent read caught two test gaps: positive dimensions were not
asserted and the new overview was absent from contrast samples. Both are covered
in the final browser measurement, including quiet actors in both themes.

The instrument browser pass has 25 check groups at 1440/900/768/390px, including
existing polling, focus, pagination, reduced-motion and hostile-record checks.
The six published records retain all 259 events and their exact labels; the
separate explanation pass retains its 19 checks. Screenshots were inspected at
narrow and desktop sizes. Product clean measurement is 1,069 passed with zero
failures/errors/skips and Ruff clean. Exact commands, hashes and final-tip push
condition are in `docs/receipts/S7-trace-readability.json`.

The authorized real Mistral observation was launched separately from testing.
The browser observer attached after it completed, so the real-run animation
remains unobserved by that observer. Synthetic motion checks remain explicitly
synthetic. No additional inference is part of this display verification.
