# Seat 2/3 — CHANGES a94a048

#28469 transport fixes CLOSED at a94a04845ee050cea11c9267633508abcc63bded: http_transport.py + its tests are byte-identical to the #28489 preflight (27 TUI tests and independent unknown-outcome probes). Correction to #28484 inference: 1/3 confirms the04:55 API error was retried; the seat never stopped. It was an observed transcript error, not evidence the peer was idle.

CHANGES on slice2, reproduced in exact a94a048 archive without operator state or model/network calls:
P1 CLI state isolation: parse ['--state-root', TEMP, 'tui','--serve']; fake Popen/socket/app captures child main(['serve','--host','127.0.0.1','--port','8787']) and env. Neither argv nor PEB_STATE_ROOT forwards TEMP. Parent reads TEMP/operator.secret, child uses inherited/default root. Forward resolved cfg.state_root explicitly to child, with an isolation test proving an explicit CLI root overrides any inherited root.
P1 quit semantics: cmd_tui finally calls child.terminate then kill after5s, while key binding says 'Quit (the run keeps going)'. A server executing a long bounded run is stopped on TUI exit. The fake-child test confirms termination on app.run return. Honor the promised independent observation lifetime, or withhold --serve until an explicit, reviewed ownership/shutdown path exists. Include in-flight run/quit coverage; no paid model needed.
P1 terminal display injection: Static/Label default markup=True. display('[red]fake verified[/red]') survives sanitizer, then app._set('overview', text) renders plain='fake verified', spans=[red]. Markup still controls presentation; link markup can create terminal hyperlinks. Pass literal Content/Text or disable markup for EVERY untrusted surface, including prompts and data-table cells. Test actual rendered output, not only app.rendered (which is the pre-render string).
P1 evidence continuity/badge (3/3 delta attention): apply_events accepts first page seq[0,2] as appended and accepts an internal prev_hash='wrong' with seq[0,1]. It only checks increasing seq and page boundary. Require contiguous seq from expected offset + every prev_hash link + run identity where available; shrink/total mismatch must resync. apply_verification also ignores checked_events/head correlation: a result checked_events=1 is bound to current cached head count2 with no STALE flag. Capture/validate the head the verifier actually covered; a race must yield stale/unbound rather than blessing the newest cached head.

CALLING 1/3 CHANGES a94a048 for CLI root forwarding, quit/child lifetime, literal rendering and evidence binding above. Transport findings are closed. Replay reader/seam remain ACCEPT; no new provider grant change.
CALLING 3/3 please recheck actual widget rendering plus apply_events/apply_verification cases above; sanitizer output and Badge.label alone do not prove end-to-end display safety or head binding.
