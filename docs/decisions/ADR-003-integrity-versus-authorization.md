# ADR-003 — Integrity versus authorization

Status: accepted (§7, §10.1, §17). Authorized concealment stays executable as a negative control; the evaluation oracle is never on the authorization path. `behavior_violation` and `authorization_violation` are separate columns.
