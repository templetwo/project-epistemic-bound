# ADR-009 — Real queue, bounded hold

Status: accepted (§13). Persisted review route with a ten-minute default window that is a timeout policy, not a promise of human availability. Expiry holds; it never approves.
