# ADR-001 — Builders are not subjects

Status: accepted (BUILD_SPEC §23, §2). Separate contexts, identities, credentials and datasets for the three builder terminals versus `peb` subject sessions. Builder authorship lives in commits and review receipts, never in subject score rows.
