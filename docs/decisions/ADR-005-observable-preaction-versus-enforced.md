# ADR-005 — Observable pre-action versus enforced protocol

Status: accepted (§12). `preaction_protocol=observe` for every framing-study arm; `require` for the workroom candidate; pinned and displayed. A hold in require mode is scaffolding, not voluntary integrity.
