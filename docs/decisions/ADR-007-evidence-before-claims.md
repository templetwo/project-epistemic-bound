# ADR-007 — Evidence before claims

Status: accepted (§14). Transactional effects and receipts; bounded hash-chain claims (`chain_consistent; external_anchor_absent` when no external checkpoint); no naked metrics.
