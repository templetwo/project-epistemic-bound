# DEFERRED — requirements that arrived after the spec (BUILD_SPEC §3.3)

Add here rather than expanding the plan. Each item: date, who raised it, what,
why not now.

**2026-09-13 browser study follow-up (Anthony's exercise notes §§14–15).**
- Evidence-linked manual report/prose review and a licensed scoped-refusal
  correctness measure: design open. Existing schema-decline can continue
  authorized work; refusal text inside an action is unscored. A human assessment
  must be distinguished from deterministic labels and from independent release
  acceptance. No automatic prose classifier or schema change is implied.
- Bounded retries for provider truncation: design open. Unlike malformed JSON,
  a truncated response carries a provider failure and remains terminal. Any
  retry policy must pin assistance, retain partial evidence and consume the
  shared budget; a study rerun requires new explicit authorization.
- The study's undispatched ninth cell does **not** need a larger call cap:
  the eighth cell failed, triggering the documented coordinator stop. The
  claimed-harmlessness completion predicate does **not** require export.
  Checkable corrections and dispositions are in
  `docs/reviews/UI-study-exercise-followup-2026-09-13.md`.

**2026-09-13 continuation annotations (seat 2/3).**
- F9: adding anchor provenance directly to the frozen `EvaluationRecord` remains
  deferred pending the interface-amendment route (ADR-020 item (b)). The existing
  runtime/event/export envelopes preserve it; both absent and supplied checkpoint
  cases are checked by
  `tests/integration/test_snapshot_evaluate.py::test_evaluation_envelope_preserves_anchor_provenance_through_export`.
  This mitigation does not close the bare-record finding.
- F21: the #28802 browser verify-handler report below is a **local chronicle,
  not externally resolvable** citation. Its substance is restated in that entry
  and in `docs/lanes/codex.md` at frozen commit `141463f`; neither is an executed
  browser race test. No independent verdict is inferred from the citation.
- Further supersession of the F14/F19 annotation below: its claim that the
  original d3f5458 warning filter made the coroutine issue a suite failure was
  disproved. `docs/receipts/S7-review-findings-2.json` records the replacement
  `PytestUnraisableExceptionWarning` guard and its failing mutation measurement.
  That receipt supersedes the old "no direct regression" residual as well.

- 2026-09-11, seat 1/3 — Full 64-hex hashes of every registered source in a
  receipt file, generated by `scripts/check_release.py` (S6). Register carries
  16-hex prefixes until then.
- 2026-09-11, seat 1/3 — Persist commitment acceptance/revision and Correction records
  in SQLite (migration 0003: `corrections` table + `UPDATE commitments`). The
  ledger is in-memory in S3; the executor persists proposals in 3/3's `commitments`
  table. Numbering: 0001 core and 0002 run_repairs are 3/3's (accepted by 1/3 at
  ef1878b to avoid churn); 0003 reserved by 1/3 (§3.1 — numbering is 1/3's).
- 2026-09-11, seat 1/3 — GateContext has no task allowlist; DefaultReferenceMonitor
  allows reads on any run-seeded resource (#27510 finding 3). Interface amendment
  candidate: `GateContext.permitted_resource_ids`. Runtime enforces the allowlist
  meanwhile.
- 2026-09-11, seat 1/3 — `EventType.review_acknowledged`. Acknowledgement is recorded as
  `review_resolved {status: acknowledged, final: false}` because `EventType` is frozen (ADR-015 §7).
  Candidate S1 amendment together with putting `checked_revision_vector` into the `gate_decided`
  payload (the held gate rebuilt from records carries `{}` for it).
- 2026-09-11, seat 1/3 — `runtime/service.py` (`WorkroomService`, `Operation`) LANDED on the lane the
  same day (see docs/lanes/claude.md); `peb serve` is wired and fails `not_implemented` until seat 2/3's
  `web.create_workroom(service, operator_secret, origin)` exists (INTERFACES §15). Remaining: the
  `["serve", …]` entry in tests/unit/test_cli_bootstrap.py's not-implemented list goes when it lands.
- 2026-09-11, seat 1/3 — DONE 13:08 EDT: `evaluation.metrics.AUTHORITY_DENY_REASONS` (2/3, main 53cb4ee)
  is the single source; the fallback in `runtime/bootstrap.py` was removed.
- 2026-09-11, seat 1/3 — DONE ~15:55 EDT: Anthony supplied G1 C1–C6 verbatim; contract_only (A2) and candidate_v1 now
  carry it, the placebo addition is length-matched to A2's (status matched). Original entry: `contract_only` (A2) carried a
  marked placeholder and was `awaiting_source_text` (not runnable as a model arm) until Anthony supplies the exact G1 C1–C6 text and
  the no-framing-exemption rule; `candidate_v1` carries the same placeholder and is runnable only as a
  labelled shakedown (manifest.settings.profile_placeholder = true). `placebo` (A3) is `draft_unmatched`
  until its addition is length-matched to the A2 addition (profile_catalog reports exact chars/words;
  tokens come from the provider's prompt_tokens per run). `check_arm_hygiene` is the EVAL-03 gate.

## Recorded at the close of the build room (2026-09-12, ADR-020)

- 2026-09-12, seat 2/3 (board #28802, restated in 2/3's final lane note at 141463f; recorded here by 1/3 at the close) - `src/peb/web/static/app.js`: the `verify` click handler reads `selectedId` inside its async body and never captures or re-checks `selectionVersion` across the await, so a delayed verify response can render into the verification pane after the operator has switched runs. `loadEvents` and `selectRun` both take that guard; `verify` does not. A view-binding gap, not a false verifier result - the returned `run_id` is still visible. Also from #28802: separate event payloads are not a linked inspector, and uncertain review responses require operator reconciliation. Not fixed before the room closed; the web lane is vacant and AGENTS.md forbids reassignment, so it is recorded here rather than moved.
- **SUPERSEDED 2026-09-13 (external review of `6d56684`, F19): fixed at `d3f5458`** — five `run_worker` sites now pass
  callables, and `pyproject.toml`'s `filterwarnings` promotes the warning class to a suite failure. The entry is kept
  because seat 2/3 reported it and the report was correct; what follows is the original text, not a live item.
  (Residual, also from that review, F14: the underlying cancelled-worker race still has no direct regression test.)
- 2026-09-12, seat 2/3 (board #28715, #28746, #28775, #28833) - two pre-existing unawaited-refresh coroutine warnings in the Textual cockpit (`src/peb/tui/`), observed by seat 2/3 in exact-archive runs at 64e4290, 9a57144 and 84b2ad1. Attested by 2/3 on the board; not re-measured by 1/3 at this entry. Never reproduced into a test, a `docs/TUI.md` limit or a matrix row - 'recorded' meant recorded on the board, which is the conversation, not the durable record.
- 2026-09-12, seat 1/3 - `docs/evidence/s6-demo/` and `docs/evidence/live-01/` keep the pre-`11551d3` `evaluation.json = {\"present\": false}` stub. The exporter was corrected at `11551d3` and `docs/evidence/deepseek-01/export-at-22a0359/` is the proof; the other bundles were never re-exported and, with seat 3/3's lane vacant, will not be by that seat. The evaluator's record for those runs remains in `events.jsonl` (`evaluation_recorded`) and `summary.json`, as their READMEs state. This is a permanent limit, not a pending follow-up.
