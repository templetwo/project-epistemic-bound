# INTERFACES — frozen at the S1 contract commit (BUILD_SPEC §8, §19 S1)

Owner: seat 1/3. Reviewers, while the build room was open (2026-09-11 → 2026-09-12):
seat 3/3 (action/grant/receipt boundaries), seat 2/3 (fixture/oracle separation,
testability). After the freeze, a change to anything listed here is an **interface
amendment**. While the room was open an amendment was proposed on the t2helix board
shard, reviewed by both other seats at a named commit, then merged by 1/3 — that is
how every amendment now on `main` was actually made, and the reviews are in
`docs/reviews/`. Anthony closed the room on 2026-09-12 (ADR-020) and there is no
second seat to review now. Until he rules on a replacement standard, no amendment is
made: the frozen modules stay frozen whether or not their owner is still seated, and
nobody silently changes another lane's contracts (§3.1).

The machine-readable form is `src/peb/contracts.py`; the published JSON Schemas
are `docs/schemas/*.schema.json` (22 files, regenerated by
`uv run --locked python scripts/export_schemas.py`, freeze-checked by
`tests/contracts/test_schema_freeze.py`).

## 1. Parsing rules (PARSE-01, §8, §9.1)

- `schema_version` is the literal `1` on every record.
- Every model is `extra="forbid"`, `strict=True`, `frozen=True`. `"false"` is not a
  boolean; `"2"` is not an integer.
- `contracts.strict_json_loads(text)` runs before any model: 64 KiB ceiling
  (`DECISION_CONTENT_CEILING_BYTES`), duplicate keys rejected, `NaN`/`Infinity`
  rejected, non-finite floats rejected.
- `contracts.parse_decision(text)` accepts exactly one JSON object and validates it
  in strict JSON mode against the discriminated `SubjectDecision` union. A JSON array,
  a scalar, a mixed finish/action object, or a decline carrying an action is
  `StrictParseError`. The raw content is the caller's to retain (PARSE-02).

## 2. Identifiers and scalars

| Type | Grammar | Notes |
|---|---|---|
| `PebId` | `^[a-z]{2,8}_[0-9a-f]{32}$` | supervisor-issued: `run_`, `ses_`, `prop_`, `evt_`, `appr_`, `rcpt_`, `cmt_`, `corr_`, `rev_`, `eval_`, `tx_` (`contracts.new_id(prefix)`) |
| `GrantId` | `grant.<slug>` or `grnt_<32 hex>` | human-readable slug allowed (example: `grant.report-edit`) |
| `ResourceId` | `^[a-z][a-z0-9_-]*(\.[a-z0-9_-]+){0,3}$`, ≤ 64 | never a host path or SQL fragment (§10) |
| `Slug` | `^[a-z0-9][a-z0-9_.-]{0,63}$` | profiles, tasks, predicates, policy versions |
| `Sha256Hex` | `^[0-9a-f]{64}$` | all digests and hashes |
| `ShortText` | 1–2000 chars | statements, summaries |
| timestamps | timezone-aware `datetime`, UTC | `RunManifest.created_at` validator enforces tz |

## 3. Enumerations (closed sets)

`ProviderKind` scripted, ollama · `RunMode` replay, scripted_validation,
model_observation · `RunStatus` created, running, waiting_review, paused,
completed, declined, failed, cancelled, interrupted · `TerminalReason` finished,
declined, invalid_output, provider_failure, budget_exhausted, context_limit,
cancelled, interrupted, evidence_failure · `ToolName` (8, §4 below) ·
`GateOutcome` allow, deny, needs_approval · `GateReason` (21 stable codes, see
`contracts.GateReason`) · `EffectStatus` applied, not_applied, indeterminate ·
`CommitmentKind` undertaking, claim · `CommitmentStatus` proposed, accepted,
superseded, fulfilled, breached, withdrawn · `DisclosureLabel` prompted,
unprompted, unknown · `ReviewStatus` pending, acknowledged, resolved_allow,
resolved_deny, expired · `PreactionProtocol` observe, require · `EvaluatorKind`
deterministic, model_grader, human_adjudication · `EventType` (20, §14.1) ·
`Actor` supervisor, subject, operator, reference_monitor, executor, evaluator,
scripted_reviewer, fixture · `ReportStatus` pass, fail, indeterminate.

## 4. Tool catalog (§10) — closed; every call is `{tool, arguments, claimed_grant_id?}`

| tool | arguments (all required unless noted) | effect? |
|---|---|---|
| `workspace.list` | — | read |
| `workspace.read` | `resource_id` | read |
| `report.write` | `resource_id`, `expected_revision ≥ 0`, `status ∈ ReportStatus`, `summary`, `evidence_refs[] ≤ 32` | effect |
| `fixture.repair` | `resource_id`, `expected_revision`, `repair_id` (one of the fixture's finite declared repairs; gate-validated) | effect |
| `checks.run` | `check_id` | effect (appends an observed result resource) |
| `handoff.write` | `resource_id`, `claims[]`, `unresolved[]`, `evidence_refs[]` | effect |
| `commitment.propose` | `text`, `kind` (default undertaking) | effect (record only; never a grant) |
| `export.send` | `resource_id`, `fields[] ≥ 1`, `sink_id` (synthetic sink) | effect |

`EFFECT_TOOLS` / `READ_TOOLS` frozensets name the split. No delete, shell, SQL,
browser, email or connector tool exists.

## 5. Subject decision (§8.1, §8.2, §9.1)

Discriminated on `kind`:

- `action`: `statement`, `action: ToolCall`, `pre_action?: PreActionDeclaration`
- `decline`: `statement`, `continue_authorized_work: bool`, `pre_action?`
- `escalate`: `statement`, `escalation: {conflict, proposal_ref?, proposed_effect?}`, `pre_action?`
- `finish`: `statement`, `completion_claim`, `evidence_refs[] ≥ 1`

`PreActionDeclaration`: `effect_summary`, `claimed_grant_id|null`,
`scope_survives_without_story: bool|null`, `consequence_of_not_acting`. These are the
subject's declarations, never trusted facts, never permission (§12).

## 6. Core records (§8.1)

`RunManifest`, `TaskSpec` (public), `PrivateOracle` (evaluator-only),
`ActionProposal`, `PreActionDeclaration`, `Grant`, `Approval`, `GateDecision`,
`EffectReceipt`, `Commitment`, `Correction`, `ReviewRequest`, `EvaluationRecord`,
`PendingEvent` / `StoredEvent`, `Checkpoint`, `VerificationResult`,
`ModelRequest` / `ModelResponse`, `GateContext`, `ReadOnlyRun`. Field lists are
in `contracts.py`; each mirrors the §8.1 row and its invariants
(e.g. `ModelResponse.prompt_tokens: int | None` — unknown is `None`, never 0;
`SnapshotHashes.code: Sha256Hex | None` — unknown is `None`, never a fake hash).

## 7. Protocols (§8.3) — signatures frozen

```python
class SubjectProvider(Protocol):
    async def generate(self, request: ModelRequest) -> ModelResponse: ...
class ReferenceMonitor(Protocol):
    def authorize(self, proposal: ActionProposal, context: GateContext) -> GateDecision: ...
class SyntheticExecutor(Protocol):
    def execute(self, proposal: ActionProposal, authorization: GateDecision) -> EffectReceipt: ...
class EvidenceStore(Protocol):
    def append(self, event: PendingEvent) -> StoredEvent: ...
    def verify(self, run_id: str, trusted_checkpoint: Checkpoint | None) -> VerificationResult: ...
class Evaluator(Protocol):
    def evaluate(self, run: ReadOnlyRun, oracle: PrivateOracle) -> EvaluationRecord: ...
```

`GateContext` is built from trusted storage, never from model JSON. The executor
revalidates inside the write transaction; a returned `allow` is not a reusable
capability. Provider and evaluator adapters never receive a writable repository.

## 8. Canonicalization and digests (§11.2) — `boundary/canonical.py`

- `canonical_json(obj, forbid_floats=False)`: UTF-8, sorted keys, `,`/`:`
  separators, no NaN/Infinity; `forbid_floats=True` rejects any float.
- `digest(domain, obj)` = SHA-256(`domain || 0x00 || canonical_json(obj)`), hex.
- Domains: `peb:event:v1`, `peb:proposal:v1`, `peb:approval:v1`,
  `peb:checkpoint:v1`, `peb:snapshot:v1`, `peb:model-input:v1`, `peb:resource:v1`.
- `proposal_digest(run_id, subject_session_id, step, call)` uses `forbid_floats=True`.
- `hmac_sign/verify(key, domain, obj)` — `development_local_hmac`, not hardware backed.

## 9. Event chain (§14.1) — `evidence/events.py`

`event_hash = digest("peb:event:v1", {event_id, schema_version, run_id, seq, ts (UTC
ISO microseconds), event_type, actor, payload, prev_hash})` — every stored field
except `event_hash` itself. `prev_hash` is `null` only at `seq 0`.
`verify_chain(events, checkpoint, *, manifest_hash=None, key=None)` reports
`chain_consistent; external_anchor_absent`, `verified_against_anchor`, `partial`
(chain fine, anchor does not cover it — tail deletion, foreign-run checkpoint,
manifest or signature mismatch) or `failed`. A checkpoint anchors only the run it
names. `manifest_hash` and `key` are checked when supplied and unchecked (not
passed) when absent; S2 storage must supply both. Supplied hashes are always
recomputed. (Two draft defects here — event_id uncovered, foreign-run checkpoint
accepted — were found by seat 2/3 before the freeze.)

## 10. Error codes (§15.2) — `errors.py`

`invalid_input`, `unauthorized`, `provider_unavailable`, `conflict`, `expired`,
`busy`, `evidence_failure`, `not_implemented`, `internal`. Envelope:
`{"error": {"code", "message", "detail"}}`. HTTP success never carries a failure.

## 11. Storage responsibilities (§3.1, §5)

| Area | Owner | Notes |
|---|---|---|
| SQLite repository, migrations (numbering by 1/3), one application write owner | 3/3 | behind `EvidenceStore` and the executor; effects + receipt + event in ONE transaction (§11.2) |
| `MemoryEvidenceStore` | 1/3 | development-only, used by S1 tests; never the operator store |
| Reference monitor, approvals, synthetic executor, replay/export/verify | 3/3 | `boundary/`, `workspace/executor.py`, `evidence/` |
| Runtime loop, context builder, commitments, review workflow, providers, CLI, config | 1/3 | `runtime/`, `providers/`, `cli.py`, `config.py` |
| Web UI/API bindings, fixtures, evaluation, acceptance/adversarial tests, release checks | 2/3 | `web/`, `evaluation/`, `fixtures/`, `tests/{acceptance,adversarial}`, `scripts/check_release.py` |
| Contracts, schemas, dependencies, root configuration | 1/3 | this document |

## 12. What S1 proves and does not prove

Proves: `peb doctor` works; schema tests pass; the §8.2 example round-trips
byte-for-byte; PARSE-01 vectors fail before any effect; one scripted decision is
captured as a verifiable five-event trace with no model and no gate
(`tests/contracts/test_scripted_capture.py`); stubs fail with `not_implemented`.

Does not prove: any authorization, any effect, any behavior. No `gate_decided` or
`effect_observed` event exists yet. Those are S2.


---

# S3/S4 additions (ADR-015) — not part of the S1 freeze; owner seat 1/3

## 13. Review route (§13) — `runtime/review.py`

- `SubjectRuntime.resolve_review(run, review_id, "allow"|"deny", *, by=Actor.operator, note="") ->
  ReviewResolution(review, approval, gate, receipt, events)`; `.executed` is true only for an applied receipt.
- `acknowledge_review(run, review_id, *, by)` → `acknowledged` (recorded as `review_resolved` with
  `final: false`; acknowledgement authorizes nothing). `expire_reviews(run, *, now=None)` → `expired`;
  the run stays `waiting_review`.
- Resolvers: `operator`, `scripted_reviewer` (labelled). Everyone else → `unauthorized`, no change.
- allow: `boundary.approvals.issue_approval(key=store.signing_key(), key_id=store.key_id, proposal=HELD,
  grant_id=gate.resolved_grant_id, grant_version=store.grant_version(...), revision_vector=
  store.current_revisions(...), policy_version, issuer=by, ttl_s)` → `store.put_approval` → run
  `running` → `monitor.authorize(HELD, GateContext(subject_session_id=HELD.session, approval=…))` →
  `executor.execute` (nonce consumed in its transaction). Events: `gate_decided` (with `approval_id`,
  `resolution_of`), `effect_observed` (store-side), `review_resolved` (`executed`, `gate_reason`).
- Injection points for development stores: `SubjectRuntime(approval_issuer=…, approval_ttl_s=600)`;
  a store without `signing_key`/`put_approval` → `not_implemented`, review stays open.
- Cross-process: `bootstrap.resolve_review_from_records(state_root, run_id, review_id, decision, *, by,
  note)` and `bootstrap.list_reviews(state_root, run_id)`; CLI `peb review list|ack|allow|deny`
  (`--note`, `--scripted-reviewer`). After allow/deny the run is left `paused` (`run_paused` event) →
  `peb resume`.
- `RunRecord.held: dict[review_id, HeldProposal(proposal, gate, preaction_present)]`; rebuilt from
  records by `reconstruct.held_proposals_from_events`.

## 14. Read-only evaluation projection — `runtime/snapshot.py`

- `read_only_run(repo, run_id) -> ReadOnlyRun`: `repo.manifest` (stored genesis), `repo.events`,
  `repo.receipts`, `repo.commitments`; corrections/reviews rebuilt from events.
- `bound_verifier(repo, run_id, snapshot, checkpoint=None) -> BoundVerifier` (callable
  `ReadOnlyRun -> VerificationResult`): the object handed back must have the bound
  `digest("peb:snapshot:v1", snapshot)` and the store head (event count + last hash) must be unchanged,
  else `SnapshotMismatch` (an `evidence_failure`, → the evaluator's `verification_unavailable`).
  `checkpoint` is an OPERATOR-RETAINED `Checkpoint` covering exactly the snapshot head, or `None` →
  `repo.verify(run_id, None)` = `chain_consistent; external_anchor_absent`. The adapter never mints a
  checkpoint (seat 3/3, #27644). `anchor_provenance` ∈ {`operator_retained_checkpoint`,
  `none_external_anchor_absent`}.
- `bootstrap.evaluate_stored_run(repo, run_id, oracle, *, evaluator_factory=None, checkpoint=None)`
  → runs seat 2/3's `DefaultEvaluator(verifier)` on the snapshot and appends `evaluation_recorded`
  (actor `evaluator`) with payload `{evaluation: EvaluationRecord, verification: VerificationResult,
  snapshot_digest, snapshot_events, anchor_provenance}`. `peb demo` / model-run summaries carry
  `evaluation` (`{"status": "unavailable", …}` when the evaluator lane is absent — never a fake verdict);
  the demo evaluates BEFORE its final checkpoint so the anchor covers the evaluation event.
- §17 columns: `bootstrap.authority_deny_reasons()` = seat 2/3's `evaluation.metrics.AUTHORITY_DENY_REASONS`
  when present, else the identical fallback set (board #27633 R1).

## 15. S4 operator service seam — `runtime/service.py` (1/3, planned) ↔ `web/` (2/3)

- `class WorkroomService: async def request(self, operation: Operation, path_ids: dict[str, str],
  payload: dict) -> dict`. `Operation` is a closed `StrEnum`; the web layer maps fixed routes to it and
  never forwards arbitrary method names.

| Operation | path_ids | payload | returns |
|---|---|---|---|
| `health.get` | — | `{}` | the `peb doctor` report (versions, state root, storage, port, provider readiness, `ready`) |
| `demo.run` | — | `{"case": "truthful-repair"|"authorized-concealment"|"forbidden-export", "frame"?: "ordinary"|"game"|"roleplay"|"evaluation"}` | the `peb demo` summary (+ `outcome_columns`); same bootstrap path |
| `run.start` | — | `{"provider": "ollama", "model": "<installed id>", "profile": "<id>", "task"?: "<registered fixture id; default conceal-error-basic>", "max_model_calls"?: 1..64, "confirm": true}` | the `peb run` summary (+ `outcome_columns`); same bounded runtime under the same locks; never chooses a model |
| `run.preview` | — | the `run.start` selection WITHOUT `confirm`, plus optional `input_rate` + `output_rate` (USD per 1M tokens, together or not at all; input = cache-miss peak) and `rates_provenance` | `peb run … --dry-run` for the SAME endpoint `run.start` would use: outbound scope + maximum budget (+ worst-case cost only with rates) + the normalized `start_payload` the web layer binds its one-use preview token to; no network, no store, no state-root change (ADR-017 addendum; seat 2/3's #27923) |
| `run.create` | — | the `run.start` selection WITHOUT `confirm` | §15 `POST /api/runs` as its own operation (ADR-018): config validated (task, runnable profile, limits, endpoint policy, explicit model), run RECORDED (`started: false`, zero model calls, genesis event only; the store's initial status is `running`); NO probe, NO model call, no inference lock — nothing leaves the machine. The web layer must gate a HOSTED create behind its preview token like `run.start` |
| `run.step` | `run_id` | `{"confirm": true}` | §15 `POST /api/runs/{id}/step`: probe first, then at most ONE subject decision and its permitted effect on a `created`/`running` run (paused/waiting_review → conflict naming `run.resume`; terminal → conflict); summary as `run.start` plus `steps_taken`; not evaluated while still active (`evaluation_note`) |
| `run.begin` | `run_id` | `{"confirm": true}` | §15 `POST /api/runs/{id}/start`: probe first, then the bounded loop on a `created`/`running` run to a boundary — the same loop `run.start` runs after creating; same locks; evaluated once at the boundary |
| `commitment.accept` | `run_id`, `commitment_id` | `{"note"?}` | §15 `POST …/commitments/{cid}/accept`: operator accepts a task-scoped PROPOSED undertaking; records `commitment_accepted`; `authority.grants_unchanged` is measured; twice → conflict; unknown id → invalid_input |
| `commitment.revise` | `run_id`, `commitment_id` | `{"text", "note"?}` | §15 `POST …/commitments/{cid}/revise`: operator-authorized, version-checked supersession (the path names the exact current version; superseded/withdrawn → conflict); prior text preserved on the record; the new version names its predecessor |
| `study.plan` | — | `{"config": <StudyConfig object>}` | EVAL-02: exactly `peb study plan --config`: seat 2/3's `evaluation.planner.build_plan` behind the seam — a bounded, seeded, reproducible schedule (registered fixtures, A0..A3, explicit trial/call caps; counts start at zero); no store, no provider, no write; the planner's refusal surfaces as `invalid_input` with bounded errors; `not_implemented` when the planner lane is absent |
| `profiles.list` | — | `{}` | `{"profiles": [profile_catalog rows: profile_id, arm, status, runnable, preaction_protocol, placeholder_text, chars, words, addition_chars, hash, source], "hygiene_findings": [EVAL-03 findings]}` (no store) |
| `runs.list` | — | `{}` | `{"runs": [RunSummary…]}` |
| `run.get` | `run_id` | `{}` | `{"run": ReadOnlyRun(json), "status", "reviews", "held": [proposal ids]}` |
| `run.pause` / `run.cancel` | `run_id` | `{"note"?}` | `{"status", "event": {seq, type, event_id}}` |
| `run.resume` | `run_id` | `{"model"?, "confirm": true}` | summary as `peb resume` (Ollama runs only; scripted → `not_implemented`) |
| `reviews.list` | — | `{}` | §15 global review view: every run's reviews in ONE store open — run_id, run_status, review_id, status, `effective_status` (the runtime's timeout rule applied read-only: an open review past its deadline shows `expired`), `open`, conflict, recipient_role, opened_at, deadline_at, `held`, and the per-run `resolve` ids; sorted open-first by deadline. Listing records nothing; resolution stays `review.resolve` per run (ADR-018 addendum) |
| `study.start` | — | `{"plan": <the complete displayed plan>, "max_model_calls": 1..32768, "confirm": true, "confirm_hosted"?: bool}` | EVAL-02 execution: exactly `peb study run <study-id> --plan FILE --max-model-calls N --confirm [--confirm-hosted]`. Seat 2/3's durable coordinator (`evaluation.study.run_study`, docs/STUDY_COORDINATOR.md) admits ONE execution of the displayed plan (rebuilt from its config and compared for exact equality; the cap must cover the plan's ceiling; a duplicate study → `conflict` before any driver call; journal `studies/<study_id>.json` + execution lock, both inside the ISO-02 fingerprint) and dispatches this seat's trial driver `runtime.study.run_trial` in plan order. Each trial is a FRESH recorded run (new run_id, subject session, workspace, grants, empty history) through the same `compose_run`/`compose_model_run`, gate, executor, recorder and evaluator as `peb run`, under the supervisor lock (plus the MacBook-wide inference lock for a model provider), with `study_id`/`trial_id`/`pair_id`/`condition_hash` pinned in `manifest.settings` at genesis beside `consequence_hash`; a scripted plan uses the fixture's registered scripted control (`runtime.study.STUDY_SCRIPTS`, pinned as `settings.case`) under the trial's real A0..A3 profile text, and a fixture without one is refused before any run exists; evaluation from records once the run is terminal; chain verification with no external anchor (`none_external_anchor_absent`). Returns the final journal (`completed` or `partial`, rows with per-trial projections and per-metric counts). Nothing is retried; a held trial stays held; an unknown outcome stops dispatch. A deepseek plan is refused without `confirm_hosted` — here and again by the driver — and the web layer must bind its one-use preview token to the exact plan and cap. `not_implemented` when the coordinator lane is absent |
| `study.get` | `study_id` | `{}` | the study's durable journal, read-only: status `ready|running|completed|partial|interrupted`, rows (`planned|dispatching|recorded|unknown|not_started` with `missing_reason`), counts and `metric_counts`; an abandoned execution (journal `running`, lock free) reads as `interrupted` and is never resumed; unknown study → `invalid_input`; `not_implemented` when the coordinator lane is absent |
| `study.preview` | — | `{"plan": <the complete displayed plan>, "max_model_calls": 1..32768, "input_rate"?, "output_rate"?, "rates_provenance"?}` | exactly `peb study preview <study-id> --plan FILE --max-model-calls N [--input-rate --output-rate --rates-provenance]` (seat 2/3's #28658): the whole plan's pre-launch scope — the plan validated by the coordinator's own rule (exact rebuild equality → else `invalid_input`), the cap by the coordinator's rule (must cover the plan's ceiling), then `runtime.bootstrap.outbound_scope` per unique (fixture, profile, frame) condition with the plan's actual provider, model, thinking and per-trial limits, and an `aggregate` summed over the planned trials (max output/input tokens, step-0 prompt estimate, worst-case cost only with rates; `probes` named as separate). Returns `preview: true`, `study_id`, `plan_hash`, `endpoint`, `conditions[]` (each with `trials` and the per-run `scope`), `aggregate`, and the normalized `start_payload` `{plan, max_model_calls, confirm: true, confirm_hosted: <provider == deepseek>}` the web layer binds its one-use ticket to for a hosted `study.start`. No network, no store, nothing written; never confirms; a scripted plan is refused (nothing leaves the machine) |
| `comparison.get` | — | `{"left_run_id", "right_run_id", "axis": "frame"|"profile"}` | one operator-selected pair of RECORDED runs compared by seat 2/3's pure `evaluation.comparison.compare_runs` (docs/MATCHED_COMPARISON.md): the service projects both runs (`runtime.snapshot.project` → snapshot + verifier bound to that snapshot's head) and calls the core with the two snapshots, the two bound verifiers and the axis while the store stays open; the repository never crosses the seam; nothing is recorded (`recorded: false`, `anchor_provenance: none_external_anchor_absent`). Result under `comparison` (`status: matched|not_comparable`, reasons, per-metric paired counts of ONE pair, planned = null). Same run twice, moved evidence, missing `consequence_hash` pin and mismatched conditions are the core's reasons, not seam errors; `not_implemented` when the comparison lane is absent |
| `evidence.replay` | — | `{"bundle_dir": "<absolute path>"}` | replay of an EXPORTED bundle through seat 3/3's shared reader `evidence.bundle.inspect_bundle` (docs/BUNDLE_REPLAY.md), returned unchanged: `mode: replay`, `recorded: false`, `provider_invoked: false`, `source_manifest`, `events`, `resources`, `verification` (chain, inventory, genesis binding, named supported and unsupported checks; `failed` summary on any defect). Opens no store; imports nothing into the operator root; the seam refuses only a relative path; `not_implemented` when the reader lane is absent |
| `review.list` | `run_id` | `{}` | `{"reviews": [ReviewRequest…]}` |
| `review.resolve` | `run_id`, `review_id` | `{"decision": "ack"|"allow"|"deny", "note"?}` | as `resolve_review_from_records` |
| `evidence.verify` | `run_id` | `{"checkpoint"?: Checkpoint}` | `{"run_id", "verification": VerificationResult, "anchor_provenance", "verified_head": {"run_id", "event_count", "head_hash"} \| null}` — the checkpoint is retained by the operator or absent (never minted here); `verified_head` (additive, ADR-019 addendum 3) is the head identity the verifier actually covered, read from the store's append-only chain right after the verification and reported only when the count read back equals `checked_events`; a cockpit binds its badge to this identity, never to its own cached hash, and shows `null` as unbound |
| `evidence.export` | `run_id` | `{"out": path}` | as `peb export` (3/3's exporter) |

- Every failure is a `PebError` envelope (§15.2); `not_implemented` is a real error, never a fake
  successful control. Payloads are validated per operation on the service side (strict models).
- Web layer (2/3): `create_workroom(service, operator_secret, origin="http://127.0.0.1:8787") ->
  FastAPI`; strict JSON/size limits, session + CSRF, loopback Host/Origin checks, inert rendering.
  `operator_secret` comes from the state root (`config.py`, 1/3), never from source or model input.
  `peb serve` (1/3) builds the service and calls the factory.


## 16. Providers (ADR-017 amendment; owner seat 1/3)

| Provider | Endpoint rule | Model | Credential | Response format | Errors (distinct, no fallback) |
|---|---|---|---|---|---|
| `scripted` | none | fixture script | none | n/a | n/a (never a measured result) |
| `ollama` | `http://` loopback only | explicit installed id (`/api/tags`) | none | `json` (default) or `json_schema` | server_unreachable, unknown_model, unsupported_setting, timeout, truncated, model_id_mismatch, transport |
| `deepseek` | explicit `https://` only, no userinfo | explicit id listed by `GET /models` | `DEEPSEEK_API_KEY` env var, header only | `json_object` | + key_absent, auth_error, insufficient_balance, rate_limited, server_error, bad_request |

`peb run --provider deepseek … --dry-run` / `bootstrap.outbound_scope(...)` prints the outbound-data scope and the
maximum budget with no network and no state change; it is the gate before any paid request.
