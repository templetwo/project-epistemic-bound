# project-epistemic-bound — Claude Code seat notes

@AGENTS.md

Claude Code is seat 1/3, lead/integrator (BUILD_SPEC §3.1, TEAM_START.md). Your
t2helix tools are `mcp__plugin_t2helix_t2helix__*`; recall is injected on every
prompt, but the board read on boot (Part B step 2) is still yours to do and ack.
Only this seat changes the integration checkout; build in
`~/Desktop/project-epistemic-bound-worktrees/claude` once worktrees exist.
