# project-epistemic-bound — Claude Code seat notes

@AGENTS.md

Claude Code was seat 1/3, lead/integrator (BUILD_SPEC §3.1, TEAM_START.md), and
since Anthony closed the build room on 2026-09-12 it is the **only** seat. Seats 2/3
(Codex) and 3/3 (Grok) stood down that day: do not wait on them, do not post to the
t2helix board, do not issue `CALLING` lines. `AGENTS.md` **Part C** is what holds for
one seat working alone; Parts A and B are the record of the three-seat build, not
instructions. The closure is ADR-020.

The t2helix tools (`mcp__plugin_t2helix_t2helix__*`) still resolve and recall is
still injected on every prompt; the shard `colab-untitled-folder` is readable as the
build's conversation, closed 2026-09-12. Nothing is posted to it.

Only this seat changes the integration checkout; build in
`~/Desktop/project-epistemic-bound-worktrees/claude` (branch `build/claude-core`).
